#version 460
layout(local_size_x=256,local_size_y=1,local_size_z=1)in;layout(set=0,binding=0)readonly buffer Input{float input_value[];};layout(set=0,binding=1)writeonly buffer Output{float output_value[];};layout(push_constant)uniform Push{uint values;float input_scale;}pc;void main(){uint index=gl_GlobalInvocationID.x;if(index>=pc.values)return;float value=input_value[index]*pc.input_scale;output_value[index]=value/(1.0+exp(-value));}
