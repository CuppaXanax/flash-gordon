#version 460
layout(local_size_x=256,local_size_y=1,local_size_z=1)in;layout(set=0,binding=0)readonly buffer Gate{float gate[];};layout(set=0,binding=1)readonly buffer Up{float up[];};layout(set=0,binding=2)writeonly buffer Output{float output_value[];};layout(push_constant)uniform Push{uint values;}pc;
void main(){uint index=gl_GlobalInvocationID.x;if(index>=pc.values)return;float g=gate[index];output_value[index]=(g/(1.0+exp(-g)))*up[index];}
